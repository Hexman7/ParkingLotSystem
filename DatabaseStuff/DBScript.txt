CREATE TABLE PARKING_LOT(
ID BIGINT AUTO_INCREMENT PRIMARY KEY,
NAME VARCHAR(255),
CITY VARCHAR(255),
STREET_ADDRESS VARCHAR(255)
)



SELECT * FROM PARKING_LOT p


CREATE TABLE PARKING_SLOT(
ID INTEGER,
PARKING_LOT_ID BIGINT FOREIGN KEY PARKING_LOT_ID REFERENCES PARKING_LOT(ID),
DISTANCE_TO_ENTRY DOUBLE_PRECISION,
IS_OCCUPIED BOOLEAN,
IS_EV_COMPATIBLE BOOLEAN
)


CREATE TABLE PARKING_SLOT (
ID INTEGER,
PARKING_LOT_ID BIGINT,
DISTANCE_TO_ENTRY DOUBLE PRECISION,
IS_OCCUPIED BOOLEAN,
IS_EV_COMPATIBLE BOOLEAN,
FOREIGN KEY (PARKING_LOT_ID) REFERENCES PARKING_LOT(ID)
)